# scs-super-admin frontend env example
# In local dev, leave this as /api/v1 if you are using a local backend rewrite.
# On Vercel, set this to your public backend URL:
# https://scs-inventory-server.onrender.com/api/v1
NEXT_PUBLIC_API_URL="https://scs-inventory-server.onrender.com/api/v1"
